<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.meinil.system.mapper.SysRoleMapper">

    <resultMap type="com.meinil.system.domain.entity.SysRole" id="SysRoleResult">
        <id     property="id"                 column="id"                    />
        <result property="roleName"           column="role_name"             />
        <result property="roleCode"           column="role_code"             />
        <result property="roleSort"           column="role_sort"             />
        <result property="status"             column="status"                />
        <result property="delFlag"            column="del_flag"              />
        <result property="createBy"           column="create_by"             />
        <result property="createTime"         column="create_time"           />
        <result property="updateBy"           column="update_by"             />
        <result property="updateTime"         column="update_time"           />
        <result property="remark"             column="remark"                />
    </resultMap>

    <sql id="BaseField">
        id, role_code, role_name, role_sort, status, remark, can_del, create_by, create_time, update_time, del_flag
    </sql>

    <select id="selectRoleByUserId" parameterType="Long" resultMap="SysRoleResult">
        select distinct r.id, r.role_name, r.role_code, r.role_sort, r.status, r.del_flag, r.create_time, r.remark
        from sys_role r
        left join sys_user_role ur on ur.role_id = r.id
        left join sys_user u on u.id = ur.user_id
        WHERE r.del_flag = '0' and ur.user_id = #{userId}
    </select>

    <select id="selectList" resultMap="SysRoleResult">
        SELECT
            <include refid="BaseField" />
        FROM
            sys_role
        WHERE
            del_flag = 0
            <if test='query.roleCode != null and query.roleCode != ""'>
                AND role_code LIKE CONCAT('%', #{query.roleCode}, '%')
            </if>
            <if test='query.roleName != null and query.roleName != ""'>
                AND role_name LIKE CONCAT('%', #{query.roleName}, '%')
            </if>
        ORDER BY role_sort ASC
    </select>
    <select id="selectRoleByRoleCode" resultType="com.meinil.system.domain.entity.SysRole">
        SELECT
            <include refid="BaseField" />
        FROM
            sys_role
        WHERE
            del_flag = 0
            <if test='roleCode != null and roleCode != ""'>
               AND role_code = #{roleCode}
            </if>
    </select>
</mapper>