<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.meinil.product.mapper.ProdSoftMapper">

    <resultMap type="com.meinil.product.domain.entity.ProdSoft" id="ProdSoftResult">
        <id     property="id"          column="id"           />
        <result property="name"        column="name"         />
        <result property="code"        column="code"         />
        <result property="version"     column="version"      />
        <result property="publishTime" column="publish_time" />
        <result property="fileIds"     column="file_ids"     />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"  column="create_time"  />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"  column="update_time"  />
        <result property="delFlag"     column="del_flag"     />
    </resultMap>

    <sql id="BaseField">
        id, name, code, version, publish_time, file_ids, create_by, create_time, update_by, update_time, del_flag
    </sql>

    <select id="selectList" resultMap="ProdSoftResult">
        SELECT
            <include refid="BaseField" />
        FROM
            prod_soft
        WHERE
            del_flag = 0
            <if test='query != null and query.name != null and query.name != ""'>
                AND name LIKE CONCAT('%', #{query.name}, '%')
            </if>
            <if test='query != null and query.code != null and query.code != ""'>
                AND code LIKE CONCAT('%', #{query.code}, '%')
            </if>
            <if test='query != null and query.version != null and query.version != ""'>
                AND version LIKE CONCAT('%', #{query.version}, '%')
            </if>
        ORDER BY update_time DESC, create_time DESC, id DESC
    </select>

    <select id="selectByCode" resultMap="ProdSoftResult">
        SELECT
            <include refid="BaseField" />
        FROM
            prod_soft
        WHERE
            del_flag = 0
            AND code = #{code}
        LIMIT 1
    </select>
</mapper>
