spring:
  application:
    name: sparion-gateway
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
  config:
    import: nacos:sparion-common.properties,nacos:sparion-gateway.properties
server:
  port: 11000